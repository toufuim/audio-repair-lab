# 驗證狀態

2026-09-24，Qwen 開源網頁版。實測硬體：Apple M1、16 GB 記憶體，CPU／4 執行緒，Python 3.12。

## 已通過

- React／TypeScript／Vite 正式建置；本機瀏覽器確認 Qwen 介面與既有案例正常顯示。
- 16 項 API、音訊與 Qwen adapter 測試全部通過，無跳過項目。
- 測試涵蓋無聲／格式／長度限制、參考與輸出分離、固定範圍補靜音與超長拒絕、長短句時間軸、未替換片段保留、重試、評分、下載、CSV 和重啟資料保存。
- 專用 Python 環境相依關係檢查通過；Mac 安裝腳本重跑成功，模型重用與校驗通過。
- 官方 Qwen 0.6B Base 真實 CPU 推論、Whisper 辨識及 Demucs 人聲分離均已執行。
- 真實兩版替換工作完成：生成、背景混音、辨識、五種 WAV 下載；下載音訊為有限值、無超出數位峰值；替換前與平移後的原音逐樣本一致。

## 聲音與耗時觀察

使用約 2.06 秒的同一角色參考音，目標為「我知道我不是一個好父親」。初次單句含模型載入約 72 秒，輸出約 9.66 秒；另一個完整兩版工作約 40 秒，輸出約 1.57／1.89 秒。這是不同種子的個別案例，不能當成跨電腦效能保證；短參考的語速與結果可能不穩定。

直接輸入繁體時，這組案例的輔助辨識多次未得到「父親」。改用簡體輸入的兩個候選皆辨識為正確台詞。因此本版在送進模型前使用 OpenCC t2s 轉換參考及目標文字，介面、案例與原始輸入仍保留繁體，CSV 記錄轉換設定。最後再用繁體介面台詞測試含轉換的完整流程：兩版共約 35.33 秒，純人聲約 1.95／1.72 秒，輔助辨識兩版均符合目標。這項觀察不能證明實際聲調或音色已合格。

使用者的真實音檔、輸出與案例留在本機，沒有放進 GitHub；公開測試使用合成波形。

## 尚未驗證／限制

- Windows、Linux、NVIDIA CUDA：有安裝與裝置設定流程，尚未做對應實機完整生成驗證。
- 1.7B Base：可設定切換，尚未在本次完整實測；MPS 未啟用。
- Qwen 沒有在本整合中精確控制秒數。選取時間不足時會拒絕輸出，不以截斷或時間伸縮假裝符合。
- SoX／Flash Attention 缺少時，上游套件可能印出提示；本次 CPU、陣列參考音輸入流程未依賴它們。
- 台灣口語、聲調、音色、背景接縫與是否可直接使用，仍須人工試聽驗收。
